@RequestMapping

- Used to define the base URL or endpoint mapping.
- Now all endpoints inside the controller start with /users.

```
@RestController
@RequestMapping("/users")
public class UserController {
}

```
- it means
```
    http://localhost:8080/users/

```